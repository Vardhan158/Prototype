import { createContext, useContext, useEffect, useState, type ReactNode } from "react";
type User = { name: string; role: "Warehouse Gate Entry & Arrival Management" | "Assembly Manager" };
const users = [{ id: "ADM-0001", email: "admin@nexuswms.com", password: "Admin@2026", name: "Nexus Warehouse Gate Entry & Arrival Management", role: "Warehouse Gate Entry & Arrival Management" as const }, { id: "PRD-1001", email: "ana@nexuswms.com", password: "work@2026", name: "Ana Fernandes", role: "Assembly Manager" as const }];
const key = "nexuswms.workcraft.session.v1";
function sharedSession(): User | null { try { const raw = document.cookie.split("; ").find((part) => part.startsWith("nexuswms_sso="))?.split("=").slice(1).join("="); if (!raw) return null; const value = JSON.parse(decodeURIComponent(raw)) as User; return value.role === "Warehouse Gate Entry & Arrival Management" || value.role === "Assembly Manager" ? value : null; } catch { return null; } }
const Context = createContext<{ user: User | null; ready: boolean; login: (id: string, password: string) => User | null; logout: () => void } | null>(null);
export function AuthProvider({ children }: { children: ReactNode }) { const [user, setUser] = useState<User | null>(null); const [ready, setReady] = useState(false); useEffect(() => { try { const shared = sharedSession(); const saved = localStorage.getItem(key); if (shared) { setUser(shared); localStorage.setItem(key, JSON.stringify(shared)); } else if (saved) setUser(JSON.parse(saved)); } finally { setReady(true); } }, []); const login = (id: string, password: string) => { const value = id.trim().toLowerCase(); const found = users.find((u) => (u.id.toLowerCase() === value || u.email === value) && u.password === password); if (!found) return null; const session: User = { name: found.name, role: found.role }; setUser(session); localStorage.setItem(key, JSON.stringify(session)); return session; }; return <Context.Provider value={{ user, ready, login, logout: () => { setUser(null); localStorage.removeItem(key); } }}>{children}</Context.Provider>; }
export function useAuth() { const value = useContext(Context); if (!value) throw new Error("useAuth must be inside AuthProvider"); return value; }
